[build]
builder = "nixpacks"

# Service 1: Santi ERP Service (Bridge/Backend)
[[services]]
name = "erp-service"
root = "apps/erp-service"
watchPaths = ["apps/erp-service/**"]
buildCommand = "npm install && npm run build"
startCommand = "npm run start"


