{
  "framework": "astro",
  "installCommand": "npm install",
  "buildCommand": "cd apps/web && npm run build && cp -r .vercel/output ../../.vercel/output",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet packages turbo.json apps/web"
}
