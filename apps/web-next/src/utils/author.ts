/**
 * Overrides or translates author names to "Khusnudhoni HM" for branding.
 */
export function getTranslatedAuthor(author: string, locale: string): string {
  // Always return the user's name for portfolio branding
  return 'Khusnudhoni HM';
}
